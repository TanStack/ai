import models from './assets/tanstack-ai.prisma?raw'

/** Filename used by the Prisma models copy CLI. */
export const prismaModelsFilename = 'tanstack-ai.prisma'

/** Provider-neutral, models-only Prisma schema fragment. */
export const prismaModels: string = models
