# @tanstack/ai-orchestration

Generator-based workflows and orchestrators for TanStack AI. Define typed agents that run via `chat()`, compose them into workflows with `yield*`, pause for approvals, and stream state through native AG-UI events.

> Status: v0 prototype.
