# Anthropic API key — the in-sandbox `claude` CLI uses an Anthropic model.
# Get yours at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Daytona API key — used to create the cloud sandbox the agent runs in.
# Get yours at: https://app.daytona.io/
DAYTONA_API_KEY=
