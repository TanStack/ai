# Optional. The example can paste a key in the browser (BYOK).
# Set this if you want the relay to use an env key when no header is sent.

# Reactor: https://www.reactor.inc/dashboard
REACTOR_API_KEY=
