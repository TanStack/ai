{
  "name": "@tanstack/sandbox-cloudflare-agent-colocated-example",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build:runner": "esbuild src/container-runner.ts --bundle --platform=node --format=esm --target=node20 --outfile=dist/container-runner.mjs",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "@cloudflare/sandbox": "^0.12.1",
    "@tanstack/ai": "workspace:*",
    "@tanstack/ai-claude-code": "workspace:*",
    "@tanstack/ai-sandbox": "workspace:*",
    "@tanstack/ai-sandbox-local-process": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260317.1",
    "@types/node": "^24.10.3",
    "esbuild": "^0.25.0",
    "wrangler": "^4.88.0"
  }
}
