{
  // The Worker + Durable Object surface (worker.ts, coordinator.ts, run-log-do.ts,
  // protocol.ts) type-checks against @cloudflare/workers-types. The in-container
  // runner (container-runner.ts) is a Node program type-checked separately by
  // tsconfig.runner.json — it uses node:http globals that conflict with the
  // Workers ones, so it must NOT share this project's lib/types.
  "files": [],
  "references": [
    { "path": "./tsconfig.worker.json" },
    { "path": "./tsconfig.runner.json" }
  ]
}
