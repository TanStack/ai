---
'@tanstack/ai-durable-stream': patch
---

Add a package README: what `durableStream` is relative to `memoryStream`, the route wiring, the options, the Cloudflare service-binding form, and what the delivery log is not.
