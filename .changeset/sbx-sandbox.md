---
'@tanstack/ai-sandbox': patch
'@tanstack/ai-sandbox-docker': minor
---

Add `sbxSandbox()` for Docker Sandboxes microVMs, and optional `adapterName` on sandbox ensure/create so network policy can allow the model API host.
