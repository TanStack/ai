---
'@tanstack/ai-memory': patch
---

Add a package README: install, `memoryMiddleware` wiring, the five adapters and their subpaths, semantic scoring, the `testkit` contract suite, and links to the docs.
