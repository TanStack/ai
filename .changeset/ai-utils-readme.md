---
'@tanstack/ai-utils': patch
---

Add a package README listing the shared helpers (`generateId`, `getApiKeyFromEnv`, the base64 helpers, `transformNullsToUndefined`, `undoNullWidening`) and when to use each.
