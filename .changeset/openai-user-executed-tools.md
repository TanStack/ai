---
'@tanstack/openai-base': patch
---

Surface OpenAI `apply_patch`, `local_shell`, and local `shell` calls for the app to run, and send the results back as `apply_patch_call_output`, `local_shell_call_output`, and `shell_call_output`.
